apiVersion: v1
kind: Pod
metadata:
  name: mi-appi
  labels:
    app: web
spec:
  containers:
  - name: front-end
    image: nginx
    ports:
    - containerPort: 80
  - name: back-end
    image: redis

  # - name: back-end-dos
  #   image: ubuntu:latest
  #   command: ["/bin/bash", "-c", "sleep infinity"]

